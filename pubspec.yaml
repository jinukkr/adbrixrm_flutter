name: adbrixrm_flutter
description: AdBrix Remastered plugin for Flutter project. AdBrix is global Attribution and Analytics tool for Android and iOS app.
version: 1.2.4
author: peter@igaworks.com / jinukkr@gmial.com
homepage: https://adbrix.io

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.igaworks.adbrixrm_flutter
        pluginClass: AdbrixrmFlutterPlugin
      ios:
        pluginClass: AdbrixrmFlutterPlugin
